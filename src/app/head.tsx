export const metadata = {
  title: 'Home',
  description: 'Nextn — Home page',
};

// head.tsx intentionally returns null — the metadata export is used by Next.js App Router.
export default function Head() {
  return null;
}
